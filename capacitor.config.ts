import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Calculo_Produto',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
